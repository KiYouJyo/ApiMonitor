<ContentDialog
    x:Class="ApiMonitor.Views.AccountEditorDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewModels="using:ApiMonitor.ViewModels"
    xmlns:converters="using:ApiMonitor.Views.Converters"
    xmlns:loc="using:ApiMonitor.Services"
    Title="{Binding Title}"
    PrimaryButtonText="{x:Bind SaveButtonText}"
    SecondaryButtonText="{x:Bind CancelButtonText}"
    DefaultButton="Primary"
    IsPrimaryButtonEnabled="{Binding CanSave}"
    PrimaryButtonClick="OnPrimaryButtonClick"
    SecondaryButtonClick="OnSecondaryButtonClick"
    CloseButtonClick="OnCloseButtonClick">

    <ContentDialog.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" />
        <converters:StatusSeverityConverter x:Key="StatusSeverityConverter" />

        <!--
          仅本对话框使用的圆角与按钮样式：
          外框 10px；保存/取消按钮共用同一圆角与 DefaultButtonStyle 尺寸基座，
          只通过 Accent/Default 基座区分主次视觉。
        -->
        <CornerRadius x:Key="AccountDialogCornerRadius">10,10,10,10</CornerRadius>

        <Style
            x:Key="AccountDialogAccentButtonStyle"
            TargetType="Button"
            BasedOn="{StaticResource AccentButtonStyle}">
            <Setter Property="CornerRadius" Value="{StaticResource AccountDialogCornerRadius}" />
        </Style>

        <Style
            x:Key="AccountDialogSecondaryButtonStyle"
            TargetType="Button"
            BasedOn="{StaticResource DefaultButtonStyle}">
            <Setter Property="CornerRadius" Value="{StaticResource AccountDialogCornerRadius}" />
        </Style>

        <!--
          局部 ContentDialog 样式：仅对本对话框生效。
          模板为 WinUI 3 默认模板的最小副本，改动只有：
          - DialogSpace 裁剪半径与外框一致（AccountDialogCornerRadius）；
          - 默认按钮状态使用上面的本地按钮样式，使保存/取消圆角一致。
        -->
        <Style
            x:Key="AccountEditorDialogStyle"
            TargetType="ContentDialog"
            BasedOn="{StaticResource DefaultContentDialogStyle}">
            <Setter Property="CornerRadius" Value="{StaticResource AccountDialogCornerRadius}" />
            <Setter Property="PrimaryButtonStyle" Value="{StaticResource AccountDialogAccentButtonStyle}" />
            <Setter Property="SecondaryButtonStyle" Value="{StaticResource AccountDialogSecondaryButtonStyle}" />
            <Setter Property="CloseButtonStyle" Value="{StaticResource AccountDialogSecondaryButtonStyle}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContentDialog">
                        <Border x:Name="Container">

                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="DialogShowingStates">

                                    <VisualStateGroup.Transitions>
                                        <VisualTransition To="DialogHidden">

                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="LayoutRoot" Storyboard.TargetProperty="Visibility">
                                                    <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="Visible" />
                                                </ObjectAnimationUsingKeyFrames>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="LayoutRoot" Storyboard.TargetProperty="IsHitTestVisible">
                                                    <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="False" />
                                                </ObjectAnimationUsingKeyFrames>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="ScaleTransform" Storyboard.TargetProperty="ScaleX">
                                                    <DiscreteDoubleKeyFrame KeyTime="0:0:0" Value="1.0" />
                                                    <SplineDoubleKeyFrame KeyTime="{StaticResource ControlFastAnimationDuration}" KeySpline="{StaticResource ControlFastOutSlowInKeySpline}" Value="1.05" />
                                                </DoubleAnimationUsingKeyFrames>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="ScaleTransform" Storyboard.TargetProperty="ScaleY">
                                                    <DiscreteDoubleKeyFrame KeyTime="0:0:0" Value="1.0" />
                                                    <SplineDoubleKeyFrame KeyTime="{StaticResource ControlFastAnimationDuration}" KeySpline="{StaticResource ControlFastOutSlowInKeySpline}" Value="1.05" />
                                                </DoubleAnimationUsingKeyFrames>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="LayoutRoot" Storyboard.TargetProperty="Opacity">
                                                    <DiscreteDoubleKeyFrame KeyTime="0:0:0" Value="1.0" />
                                                    <LinearDoubleKeyFrame KeyTime="{StaticResource ControlFasterAnimationDuration}" Value="0.0" />
                                                </DoubleAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualTransition>
                                        <VisualTransition To="DialogShowing">

                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="LayoutRoot" Storyboard.TargetProperty="Visibility">
                                                    <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="Visible" />
                                                </ObjectAnimationUsingKeyFrames>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="ScaleTransform" Storyboard.TargetProperty="ScaleX">
                                                    <DiscreteDoubleKeyFrame KeyTime="0:0:0" Value="1.05" />
                                                    <SplineDoubleKeyFrame KeyTime="{StaticResource ControlNormalAnimationDuration}" KeySpline="{StaticResource ControlFastOutSlowInKeySpline}" Value="1.0" />
                                                </DoubleAnimationUsingKeyFrames>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="ScaleTransform" Storyboard.TargetProperty="ScaleY">
                                                    <DiscreteDoubleKeyFrame KeyTime="0:0:0" Value="1.05" />
                                                    <SplineDoubleKeyFrame KeyTime="{StaticResource ControlNormalAnimationDuration}" KeySpline="{StaticResource ControlFastOutSlowInKeySpline}" Value="1.0" />
                                                </DoubleAnimationUsingKeyFrames>
                                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="LayoutRoot" Storyboard.TargetProperty="Opacity">
                                                    <DiscreteDoubleKeyFrame KeyTime="0:0:0" Value="0.0" />
                                                    <LinearDoubleKeyFrame KeyTime="{StaticResource ControlFasterAnimationDuration}" Value="1.0" />
                                                </DoubleAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualTransition>
                                    </VisualStateGroup.Transitions>
                                    <VisualState x:Name="DialogHidden" />
                                    <VisualState x:Name="DialogShowing">
                                        <VisualState.Setters>
                                            <Setter Target="PrimaryButton.IsTabStop" Value="True" />
                                            <Setter Target="SecondaryButton.IsTabStop" Value="True" />
                                            <Setter Target="CloseButton.IsTabStop" Value="True" />
                                            <Setter Target="LayoutRoot.Visibility" Value="Visible" />
                                            <Setter Target="BackgroundElement.TabFocusNavigation" Value="Cycle" />

                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="DialogShowingWithoutSmokeLayer">
                                        <VisualState.Setters>
                                            <Setter Target="PrimaryButton.IsTabStop" Value="True" />
                                            <Setter Target="SecondaryButton.IsTabStop" Value="True" />
                                            <Setter Target="CloseButton.IsTabStop" Value="True" />
                                            <Setter Target="LayoutRoot.Visibility" Value="Visible" />
                                            <Setter Target="LayoutRoot.Background" Value="{x:Null}" />

                                        </VisualState.Setters>
                                    </VisualState>

                                </VisualStateGroup>
                                <VisualStateGroup x:Name="DialogSizingStates">
                                    <VisualState x:Name="DefaultDialogSizing" />
                                    <VisualState x:Name="FullDialogSizing">
                                        <VisualState.Setters>
                                            <Setter Target="BackgroundElement.VerticalAlignment" Value="Stretch" />

                                        </VisualState.Setters>
                                    </VisualState>

                                </VisualStateGroup>
                                <VisualStateGroup x:Name="ButtonsVisibilityStates">
                                    <VisualState x:Name="AllVisible">
                                        <VisualState.Setters>
                                            <Setter Target="FirstSpacer.Width" Value="{ThemeResource ContentDialogButtonSpacing}" />
                                            <Setter Target="SecondaryColumn.Width" Value="*" />
                                            <Setter Target="SecondaryButton.(Grid.Column)" Value="2" />

                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="NoneVisible">
                                        <VisualState.Setters>
                                            <Setter Target="CommandSpace.Visibility" Value="Collapsed" />

                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="PrimaryVisible">
                                        <VisualState.Setters>
                                            <Setter Target="PrimaryButton.(Grid.Column)" Value="4" />
                                            <Setter Target="SecondaryButton.Visibility" Value="Collapsed" />
                                            <Setter Target="CloseButton.Visibility" Value="Collapsed" />

                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="SecondaryVisible">
                                        <VisualState.Setters>
                                            <Setter Target="SecondaryButton.(Grid.Column)" Value="4" />
                                            <Setter Target="PrimaryButton.Visibility" Value="Collapsed" />
                                            <Setter Target="CloseButton.Visibility" Value="Collapsed" />

                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="CloseVisible">
                                        <VisualState.Setters>
                                            <Setter Target="PrimaryButton.Visibility" Value="Collapsed" />
                                            <Setter Target="SecondaryButton.Visibility" Value="Collapsed" />

                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="PrimaryAndSecondaryVisible">
                                        <VisualState.Setters>
                                            <Setter Target="SecondaryButton.(Grid.Column)" Value="4" />
                                            <Setter Target="CloseButton.Visibility" Value="Collapsed" />

                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="PrimaryAndCloseVisible">
                                        <VisualState.Setters>
                                            <Setter Target="SecondaryButton.Visibility" Value="Collapsed" />

                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="SecondaryAndCloseVisible">
                                        <VisualState.Setters>
                                            <Setter Target="PrimaryButton.Visibility" Value="Collapsed" />

                                        </VisualState.Setters>
                                    </VisualState>

                                </VisualStateGroup>
                                <VisualStateGroup x:Name="DefaultButtonStates">
                                    <VisualState x:Name="NoDefaultButton" />
                                    <VisualState x:Name="PrimaryAsDefaultButton">
                                        <VisualState.Setters>
                                            <Setter Target="PrimaryButton.Style" Value="{StaticResource AccountDialogAccentButtonStyle}" />

                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="SecondaryAsDefaultButton">
                                        <VisualState.Setters>
                                            <Setter Target="SecondaryButton.Style" Value="{StaticResource AccountDialogSecondaryButtonStyle}" />

                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="CloseAsDefaultButton">
                                        <VisualState.Setters>
                                            <Setter Target="CloseButton.Style" Value="{StaticResource AccountDialogSecondaryButtonStyle}" />

                                        </VisualState.Setters>
                                    </VisualState>

                                </VisualStateGroup>
                                <VisualStateGroup x:Name="DialogBorderStates">
                                    <VisualState x:Name="NoBorder" />
                                    <VisualState x:Name="AccentColorBorder">
                                        <VisualState.Setters>
                                            <Setter Target="BackgroundElement.BorderBrush" Value="{ThemeResource SystemControlForegroundAccentBrush}" />

                                        </VisualState.Setters>
                                    </VisualState>

                                </VisualStateGroup>

                            </VisualStateManager.VisualStateGroups>
                            <Grid x:Name="LayoutRoot" Visibility="Collapsed">
                                <Rectangle x:Name="SmokeLayerBackground" Fill="{ThemeResource ContentDialogSmokeFill}" />
                                <Border
                                    x:Name="BackgroundElement"
                                    Background="{TemplateBinding Background}"
                                    FlowDirection="{TemplateBinding FlowDirection}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BackgroundSizing="InnerBorderEdge"
                                    CornerRadius="{TemplateBinding CornerRadius}"
                                    MinWidth="{ThemeResource ContentDialogMinWidth}"
                                    MaxWidth="{ThemeResource ContentDialogMaxWidth}"
                                    MinHeight="{ThemeResource ContentDialogMinHeight}"
                                    MaxHeight="{ThemeResource ContentDialogMaxHeight}"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    RenderTransformOrigin="0.5,0.5">
                                    <Border.RenderTransform>
                                        <ScaleTransform x:Name="ScaleTransform" />
                                    </Border.RenderTransform>
                                    <Grid x:Name="DialogSpace" CornerRadius="{StaticResource AccountDialogCornerRadius}">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <ScrollViewer
                                            x:Name="ContentScrollViewer"
                                            HorizontalScrollBarVisibility="Disabled"
                                            VerticalScrollBarVisibility="Disabled"
                                            ZoomMode="Disabled"
                                            IsTabStop="False">
                                            <Grid
                                                Background="{ThemeResource ContentDialogTopOverlay}"
                                                Padding="{ThemeResource ContentDialogPadding}"
                                                BorderThickness="{ThemeResource ContentDialogSeparatorThickness}"
                                                BorderBrush="{ThemeResource ContentDialogSeparatorBorderBrush}">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="*" />
                                                </Grid.RowDefinitions>
                                                <ContentControl
                                                    x:Name="Title"
                                                    Margin="{ThemeResource ContentDialogTitleMargin}"
                                                    Content="{TemplateBinding Title}"
                                                    ContentTemplate="{TemplateBinding TitleTemplate}"
                                                    FontSize="20"
                                                    FontFamily="{StaticResource ContentControlThemeFontFamily}"
                                                    FontWeight="SemiBold"
                                                    Foreground="{TemplateBinding Foreground}"
                                                    HorizontalAlignment="Left"
                                                    VerticalAlignment="Top"
                                                    IsTabStop="False">
                                                    <ContentControl.Template>
                                                        <ControlTemplate TargetType="ContentControl">
                                                            <ContentPresenter
                                                                Content="{TemplateBinding Content}"
                                                                MaxLines="2"
                                                                TextWrapping="Wrap"
                                                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                Margin="{TemplateBinding Padding}"
                                                                ContentTransitions="{TemplateBinding ContentTransitions}"
                                                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                                        </ControlTemplate>
                                                    </ContentControl.Template>
                                                </ContentControl>
                                                <ContentPresenter
                                                    x:Name="Content"
                                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                                    Content="{TemplateBinding Content}"
                                                    FontSize="{StaticResource ControlContentThemeFontSize}"
                                                    FontFamily="{StaticResource ContentControlThemeFontFamily}"
                                                    Foreground="{TemplateBinding Foreground}"
                                                    Grid.Row="1"
                                                    TextWrapping="Wrap" />
                                            </Grid>
                                        </ScrollViewer>
                                        <Grid
                                            x:Name="CommandSpace"
                                            Grid.Row="1"
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Bottom"
                                            XYFocusKeyboardNavigation="Enabled"
                                            Padding="{ThemeResource ContentDialogPadding}"
                                            Background="{TemplateBinding Background}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition x:Name="PrimaryColumn" Width="*" />
                                                <ColumnDefinition x:Name="FirstSpacer" Width="0" />
                                                <ColumnDefinition x:Name="SecondaryColumn" Width="0" />
                                                <ColumnDefinition x:Name="SecondSpacer" Width="{ThemeResource ContentDialogButtonSpacing}" />
                                                <ColumnDefinition x:Name="CloseColumn" Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Button
                                                x:Name="PrimaryButton"
                                                IsTabStop="False"
                                                Content="{TemplateBinding PrimaryButtonText}"
                                                IsEnabled="{TemplateBinding IsPrimaryButtonEnabled}"
                                                Style="{TemplateBinding PrimaryButtonStyle}"
                                                ElementSoundMode="FocusOnly"
                                                HorizontalAlignment="Stretch" />
                                            <Button
                                                x:Name="SecondaryButton"
                                                IsTabStop="False"
                                                Content="{TemplateBinding SecondaryButtonText}"
                                                IsEnabled="{TemplateBinding IsSecondaryButtonEnabled}"
                                                Style="{TemplateBinding SecondaryButtonStyle}"
                                                ElementSoundMode="FocusOnly"
                                                HorizontalAlignment="Stretch" />
                                            <Button
                                                x:Name="CloseButton"
                                                IsTabStop="False"
                                                Grid.Column="4"
                                                Content="{TemplateBinding CloseButtonText}"
                                                Style="{TemplateBinding CloseButtonStyle}"
                                                ElementSoundMode="FocusOnly"
                                                HorizontalAlignment="Stretch" />
                                        </Grid>

                                    </Grid>
                                </Border>

                            </Grid>
                        </Border>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </ContentDialog.Resources>

    <ScrollViewer MaxHeight="560" VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="12" MinWidth="420" MaxWidth="620">
            <ComboBox
                Header="Provider"
                ItemsSource="{Binding Providers}"
                DisplayMemberPath="DisplayName"
                SelectedValuePath="ProviderId"
                SelectedValue="{Binding SelectedProviderId, Mode=TwoWay}" />
            <TextBlock
                Text="{Binding ProviderDescription}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                TextWrapping="Wrap" />
            <RadioButtons
                loc:Loc.Key="Dialog.CredentialMode"
                ItemsSource="{Binding CredentialOptions}"
                SelectedIndex="{Binding CredentialModeIndex, Mode=TwoWay}"
                Visibility="{Binding ShowCredentialModeSelector, Converter={StaticResource BoolToVisibilityConverter}}">
                <RadioButtons.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Spacing="2">
                            <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" />
                            <TextBlock
                                Text="{Binding Description}"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                TextWrapping="Wrap" />
                        </StackPanel>
                    </DataTemplate>
                </RadioButtons.ItemTemplate>
            </RadioButtons>
            <TextBlock
                loc:Loc.Key="Dialog.CredentialModeChanged"
                Foreground="{ThemeResource SystemFillColorCautionBrush}"
                TextWrapping="Wrap"
                Visibility="{Binding ModeChangedRequiresRetest, Converter={StaticResource BoolToVisibilityConverter}}" />
            <TextBox
                loc:Loc.Key="Dialog.AccountName"
                Text="{Binding DisplayName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                PlaceholderText="{x:Bind AccountNamePlaceholderText}" />
            <ItemsControl
                ItemsSource="{Binding ConfigFieldItems}"
                Visibility="{Binding ShowConfigFields, Converter={StaticResource BoolToVisibilityConverter}}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="viewModels:ProviderConfigFieldItem">
                        <StackPanel Spacing="4" Margin="0,0,0,10">
                            <TextBlock Text="{x:Bind Label}" FontWeight="SemiBold" />
                            <TextBox
                                Text="{x:Bind Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                PlaceholderText="{x:Bind Placeholder}"
                                Visibility="{x:Bind IsBoolean, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}" />
                            <ToggleSwitch
                                IsOn="{x:Bind BooleanValue, Mode=TwoWay}"
                                OnContent="{x:Bind OnContentText}"
                                OffContent="{x:Bind OffContentText}"
                                Visibility="{x:Bind IsBoolean, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                            <TextBlock
                                Text="{x:Bind Hint}"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                TextWrapping="Wrap" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- v0.9.0：多槽位凭据（Key+SK / Basic 用户名+密码 / Bearer / QueryToken） -->
            <ItemsControl
                ItemsSource="{Binding CredentialSlotItems}"
                Visibility="{Binding ShowCredentialSlots, Converter={StaticResource BoolToVisibilityConverter}}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="viewModels:CredentialSlotItem">
                        <StackPanel
                            Spacing="4"
                            Margin="0,0,0,10"
                            Visibility="{x:Bind IsVisible, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock Text="{x:Bind Label}" FontWeight="SemiBold" />
                            <PasswordBox
                                PasswordChanged="OnCredentialSlotPasswordChanged"
                                PlaceholderText="{x:Bind Placeholder}"
                                Visibility="{x:Bind IsSecret, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                            <TextBox
                                Text="{x:Bind Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                PlaceholderText="{x:Bind Placeholder}"
                                Visibility="{x:Bind IsSecret, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}" />
                            <TextBlock
                                Text="{x:Bind Hint}"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                TextWrapping="Wrap" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock
                loc:Loc.Key="Dialog.CredentialHint"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                TextWrapping="Wrap"
                Visibility="{Binding ShowKeepCredentialHint, Converter={StaticResource BoolToVisibilityConverter}}" />
            <TextBlock
                loc:Loc.Key="Dialog.NoStoredCredentialHint"
                Foreground="{ThemeResource SystemFillColorCautionBrush}"
                TextWrapping="Wrap"
                Visibility="{Binding ShowMissingCredentialHint, Converter={StaticResource BoolToVisibilityConverter}}" />
            <TextBlock
                loc:Loc.Key="Dialog.ProviderChangedRequiresRetest"
                Foreground="{ThemeResource SystemFillColorCautionBrush}"
                TextWrapping="Wrap"
                Visibility="{Binding ProviderChangedRequiresRetest, Converter={StaticResource BoolToVisibilityConverter}}" />
            <TextBlock
                loc:Loc.Key="Dialog.ProbeConsumesQuota"
                Foreground="{ThemeResource SystemFillColorCautionBrush}"
                TextWrapping="Wrap"
                Visibility="{Binding ShowProbeConsumesQuotaHint, Converter={StaticResource BoolToVisibilityConverter}}" />

            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button Content="{x:Bind TestConnectionText}" Command="{Binding TestCommand}" />
                <ProgressRing Width="20" Height="20" IsActive="{Binding IsTesting}" />
            </StackPanel>

            <InfoBar
                IsOpen="{Binding HasTestResult}"
                Severity="{Binding TestSeverity, Converter={StaticResource StatusSeverityConverter}}"
                Title="{Binding TestTitle}"
                Message="{Binding TestResultText}" />

            <ToggleSwitch
                loc:Loc.Key="Dialog.AutoRefresh"
                OnContent="{x:Bind OnContentText}"
                OffContent="{x:Bind OffContentText}"
                IsOn="{Binding AutoRefreshEnabled, Mode=TwoWay}" />
            <ComboBox
                loc:Loc.Key="Dialog.RefreshInterval"
                ItemsSource="{Binding RefreshIntervals}"
                SelectedItem="{Binding RefreshIntervalMinutes, Mode=TwoWay}" />
            <TextBlock
                loc:Loc.Key="Dialog.RefreshIntervalHint"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                TextWrapping="Wrap" />

            <TextBlock loc:Loc.Key="Dialog.ThresholdTitle" Style="{StaticResource SubtitleTextBlockStyle}" />
            <ItemsControl ItemsSource="{Binding ThresholdItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="viewModels:ThresholdEditorItem">
                        <Grid
                            Margin="0,4"
                            ColumnDefinitions="Auto,*,Auto,Auto"
                            ColumnSpacing="12">
                            <StackPanel Grid.Column="0" Spacing="1" VerticalAlignment="Center">
                                <TextBlock
                                    Text="{x:Bind DisplayName}"
                                    FontWeight="SemiBold" />
                                <TextBlock
                                    Text="{x:Bind Unit}"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                            <ToggleSwitch
                                Grid.Column="1"
                                IsOn="{x:Bind IsEnabled, Mode=TwoWay}"
                                OnContent=""
                                OffContent="" />
                            <TextBox
                                Grid.Column="2"
                                Width="110"
                                Text="{x:Bind ThresholdText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                PlaceholderText="0.00"
                                VerticalAlignment="Center" />
                            <StackPanel Grid.Column="3" Spacing="2" VerticalAlignment="Center">
                                <TextBlock Text="{x:Bind CurrentBalanceLine, Mode=OneWay}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <TextBlock Text="{x:Bind StatusLine, Mode=OneWay}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock
                loc:Loc.Key="Dialog.ThresholdEmpty"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                TextWrapping="Wrap"
                Visibility="{Binding ShowThresholdEmptyHint, Converter={StaticResource BoolToVisibilityConverter}}" />

            <TextBlock loc:Loc.Key="Dialog.NotificationTitle" Style="{StaticResource SubtitleTextBlockStyle}" />
            <ComboBox
                loc:Loc.Key="Dialog.AccountNotification"
                ItemsSource="{Binding NotificationEnabledOptions}"
                DisplayMemberPath="Label"
                SelectedValuePath="Value"
                SelectedIndex="{Binding NotificationEnabledIndex, Mode=TwoWay}" />
            <ComboBox
                loc:Loc.Key="Dialog.RepeatInterval"
                ItemsSource="{Binding NotificationRepeatOptions}"
                DisplayMemberPath="Label"
                SelectedValuePath="Value"
                SelectedIndex="{Binding RepeatIntervalIndex, Mode=TwoWay}" />
            <ComboBox
                loc:Loc.Key="Dialog.RecoveryNotifications"
                ItemsSource="{Binding RecoveryOptions}"
                DisplayMemberPath="Label"
                SelectedValuePath="Value"
                SelectedIndex="{Binding RecoveryEnabledIndex, Mode=TwoWay}" />
            <TextBlock
                loc:Loc.Key="Dialog.RefreshNote"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                TextWrapping="Wrap" />

            <TextBlock
                Text="{Binding ValidationMessage}"
                Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                Visibility="{Binding HasValidationMessage, Converter={StaticResource BoolToVisibilityConverter}}" />
        </StackPanel>
    </ScrollViewer>
</ContentDialog>
